amends "package://github.com/jdx/hk/releases/download/v1.56.1/hk@1.56.1#/Config.pkl"

min_hk_version = "1.56.1"

local projectSteps = new Mapping<String, Step> {
  // `zig fmt` also formats `.zon`. Running it over the files hk passes in keeps
  // the ignored dependency trees (`zig-pkg/`, `.zig-cache/`, `zig-out/`, and the
  // nested `test/fixtures/build_api/zig-pkg/`) out of the check.
  ["fmt"] {
    glob = List("**/*.zig", "**/*.zon")
    stage = List("**/*.zig", "**/*.zon")
    check = "zig fmt --check {{files}}"
    fix = "zig fmt {{files}}"
  }
  // Keep hooks deterministic and independent of Docker, network access, or a
  // running database. Runtime and end-to-end coverage belongs to `mise run ci`.
  ["test-offline"] {
    check = "zig build test-offline"
    exclusive = true
    depends = "fmt"
  }
}

hooks {
  ["pre-commit"] {
    stash = "git"
    steps = projectSteps
  }
  ["check"] {
    steps = projectSteps
  }
  ["fix"] {
    steps = projectSteps
  }
}
